# ===== Arrival Ready AI Service ｜ 模型凭据配置 =====
# 复制本文件为 .env 并填入真实值：  copy .env.template .env
# .env 已被 .gitignore 覆盖，绝不入库（docs/03 §4.3）。

# 阶跃星辰开放平台 API Key（platform.stepfun.com → API 管理 → 新建）
MODEL_API_KEY=

# 开放平台接口地址（一般不用改）
MODEL_BASE_URL=https://api.stepfun.com/v1

# 视觉模型 id（有额度的 vision 模型，如 step-1v-8k；不确定可留空，
# 首次冒烟时会先查询 /models 列出可用模型再选定）
MODEL_ID=
